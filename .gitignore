# Dependencies
node_modules/
package-lock.json
yarn.lock
# pnpm-lock.yaml is committed on purpose. Vercel builds with no lockfile resolve
# caret ranges fresh, which already broke a deploy on a react minor release.

# Build output
dist/
dist-ssr/
build/
.next/
out/

# Environment
.env
.env.local
.env.*.local
.env.production.local

# IDE & editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-workspace

# Development
.cache/
*.tsbuildinfo

# Testing
coverage/

# Temporary
tmp/
temp/
*.log

# RenderCV (dev-only CV tooling, see tools/generate-cv.mjs)
.venv-cv/
rendercv_output/
public/cv/*.typ
public/cover-letter/*.typ

# Vercel CLI link (project and org ids)
.vercel/
